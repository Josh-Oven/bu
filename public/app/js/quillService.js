angular.module('bu').service('quillService', ($http) => {

  // this.getBlog = (title) => {
  //   return $http.get('../storage.json/' + title).then( (response) => {
  //     return response;
  //     console.log(response)
  //   })
  // }
  //
  // this.getBlog();

})
